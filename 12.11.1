#Програма удаляет из массива все простые числа, встречающиеся больше 1 раза.
#я не смогла оперировать лишь теми знаниями, которые у меня были, поэтому пришлось изучить функции.
#если появятся сомнения, что это писала я, то могу объяснить решение, что изначает и где применяется та или иная функция.
def isprime(a):
  
  for i in range(2, int(a**0.5)+ 1):
    if a%i==0:
      return 0
  return 1
n=int(input())
b=list(input().split())
new=[]
for nb in b:
  if str(b).count(nb)==2:
    if not isprime(int(nb)):
      new.append(nb)
  else:
    new.append(nb)
print(*new)
